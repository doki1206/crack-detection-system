# 穹眸瞰陷 · 无人机地表裂缝智能检测系统 — 深度评估报告

评估日期：2026-08-14 ｜ 评估范围：`检测系统/` 全部源码（backend + frontend）、配置、git 仓库卫生

---

## 一、系统概览

| 项 | 现状 |
|---|---|
| 定位 | 基于多模态大模型（豆包 doubao-seed-2.0-pro）零样本识别矿山地表沉陷裂缝 |
| 架构 | Vue 3 + Vite 前端（5173）→ Vite 代理 `/api` → Express 后端（3000）→ 火山引擎 Ark API |
| 代码规模 | 后端单文件 270 行；前端 src 约 4030 行（4 个页面 + 1 个 composable） |
| 核心链路 | 图片上传 → Base64 → LLM 零样本检测 → JSON 容错解析 → node-canvas 服务端标注 → 前端 Canvas 二次标注/交互 → html2canvas + jsPDF 导出报告 |
| 版本管理 | git，2 次提交，39 个跟踪文件，`.env` 已正确忽略，无密钥泄露 |

**一句话结论：作为一个暑期实践的演示原型，完成度和工程质量高于预期；但作为论文的支撑系统，"检测结果的科学有效性"存在根本性风险，是评估中最需要正视的问题。**

---

## 二、总体评分

| 维度 | 评分（10 分制） | 说明 |
|---|---|---|
| 功能完成度 | 8 | 检测、标注、历史、PDF 导出、3D 展示页全链路可用 |
| 架构设计 | 6.5 | 前后端分离清晰，但后端无分层、前端单组件过大 |
| 代码质量 | 7 | JSDoc 完善、容错设计好；但存在上帝组件、硬编码 |
| 安全性 | 4 | 无鉴权、CORS 全开、serve.js 路径穿越 |
| 性能 | 6 | 无懒加载、首屏包过大；同步 canvas 阻塞 |
| 可维护性/工程化 | 4.5 | 零测试、零 CI、无类型检查、无 lint 配置 |
| **检测科学有效性** | **3** | **LLM 零样本坐标回归不可靠，多项地质指标无法从单张 RGB 图得出** |
| 文档 | 7.5 | README 清晰可复现 |

---

## 三、核心风险：检测有效性问题（最重要）

这是整个系统作为"研究项目"的阿喀琉斯之踵，必须在论文中诚实处理。

### 3.1 坐标级输出不可靠

`server.js` 的 SYSTEM_PROMPT 要求通用多模态大模型直接输出 `bbox`（像素坐标）、`contour`（边缘关键点）。通用 VLM 的坐标回归能力是**未经校准的估算**，不是真正的目标检测——同一模型对同一张图多次调用，坐标可能漂移；与真实裂缝掩膜之间没有 IoU 保证。这意味着：

- 无法计算论文需要的 mAP、IoU、Precision/Recall 等标准检测指标；
- 无法与 U-Net、YOLO、Mask R-CNN 等基线方法做公平对比。

### 3.2 部分指标在原理上不可能从单张 RGB 图得出

Prompt 要求模型输出：

- `fractal_dimension`（分形维数）— 需要对裂缝二值掩膜做盒计数法计算，LLM 不可能"看"出来，**必然是编造值**；
- `attitude`（产状：倾向/倾角）— 需要 DEM 或多视角立体视觉，单张垂直航拍图无法测量倾角；
- `crack_density`（裂纹密度）— 需要真实地理尺度（GSD 标定），系统未做严格标定（前端 GSD 系数 120/125 为硬编码经验值）；
- `confidence` — LLM 自报的置信度不是校准概率，无统计意义。

评审老师只要追问一句"分形维数是怎么算的"，就会暴露。**这些字段目前是大模型的幻觉包装成了结构化数据。**

### 3.3 建议的补救路径（按工作量排序）

1. **最低成本**：论文中把系统重新定位为"零样本快速筛查 + 人工复核"的辅助工具，删去或弱化分形维数、产状等字段，只保留 LLM 相对可靠的能力（有无裂缝、大致位置、定性描述）；
2. **中等成本**：引入真实 CV 模型做定量部分——用开源裂缝分割模型（如 CrackFormer、DeepCrack，或用矿区实拍图微调 U-Net）输出掩膜，长度/宽度/面积/密度/分形维数全部由掩膜**计算**得出，LLM 只负责语义描述和报告生成。这条路线论文价值高得多；
3. **加分项**：采集 30~50 张大柳塔实拍图做人工标注小数据集，哪怕只报告一个小样本的准确率/召回率，也比零实证强。

---

## 四、后端评估（backend/server.js，270 行）

### 优点

- **文档质量罕见地好**：每个函数有 JSDoc，typedef 定义了数据结构，新成员上手成本低；
- **JSON 提取三层容错**（去 markdown → JSON.parse → 正则兜底，`extractJSON` L87-106）是对 LLM 输出的正确防御姿势；
- `normalizeCracks` 给所有字段兜底默认值，前端不会因缺字段崩溃；
- 标注绘制有细节：线宽/字号随图片尺寸自适应（L203/208）、轮廓首尾距离判断闭合（L229-235）；
- API 密钥未配置时返回明确错误（L148-150），密钥未泄露进 git。

### 问题

| 严重度 | 问题 | 位置 |
|---|---|---|
| 高 | **无任何鉴权 + CORS 全开**：后端一旦部署到公网，任何人可白嫖你的火山引擎 API 额度 | L19 |
| 高 | **无速率限制**，`limit: '50mb'` 的 JSON body 容易被大请求打爆内存 | L20 |
| 中 | 无请求重试/降级：Ark API 抖动或 60s 超时直接 500，用户体验是"检测失败" | L169 |
| 中 | `console.log` 打印 AI 完整返回 + 解析结果，生产环境会刷爆日志且可能泄露图片相关敏感信息 | L174-175, 249-250 |
| 中 | `node-canvas` 是原生依赖，部署/跨平台编译是常见踩坑点；且标注图绘制在主线程同步执行，大图会阻塞事件循环 | L186-243 |
| 低 | Express 5 + CommonJS，混用无碍但 `package.json` 的 `main: index.js` 与实际入口不符 | package.json L5 |
| 低 | 标注逻辑其实可以整体前移到前端（前端已有 Canvas 对齐绘制能力），省掉 canvas 依赖和一次全图 base64 回传（响应体翻倍） | — |

---

## 五、前端评估（frontend/src，约 4030 行）

### 优点

- `useThreeEarth.js`（695 行）资源管理规范：`disposeThreeObj` 递归释放 + `forceContextLoss`，Three.js 内存泄漏防护到位；
- DetectPage 的错误处理分级细致：网络错误 / 413 / API 密钥未配置 / JSON 解析失败分别给出不同提示（L570-595）；
- 结果画布用 `ResizeObserver` 按 `object-fit:contain` 精确对齐重绘，bbox 坐标换算正确——这是很多同类 demo 会做错的地方；
- 历史记录 localStorage 持久化，带超配额降级重试（L779-843），考虑了边界情况；
- IntersectionObserver 揭示动画，页面完成度高。

### 问题

| 严重度 | 问题 |
|---|---|
| 高 | **DetectPage.vue 1475 行上帝组件**：上传、校准、画布绘制、tooltip、历史、PDF 导出六个职责应拆分为子组件/composable |
| 高 | **serve.js 路径穿越漏洞**：`path.join(dist, u)` 未校验 `..`，且硬编码绝对路径，这个文件不应出现在交付物里，直接用 `vite preview` 或 nginx |
| 中 | 无路由懒加载：DetectPage 引入的 html2canvas + jsPDF（合计约 500KB+）全部进入首屏包，首页打开慢 |
| 中 | 安全评分（L442-459）和 GSD 换算（L396-398）是纯前端启发式 + 硬编码系数，数值看着专业实则无标定依据，论文引用需谨慎 |
| 中 | PDF 导出只截右栏结果区，**未包含带标注的检测图**——报告最核心的证据缺失 |
| 低 | 无 404 路由、无路由守卫；仅 900px 一档响应式断点；tooltip/dropzone 无键盘可访问性；`!important` 滥用；`javascript:void(0)` 死链 |
| 低 | 历史记录无数据版本校验，旧结构数据恢复可能异常 |

---

## 六、工程化与协作

- **零测试**：前后端均无一个测试文件，`npm test` 是占位符。至少应给 `extractJSON` / `normalizeCracks` 这类纯函数补单元测试（Vitest，半天工作量）；
- **无 lint / 格式化配置**：无 ESLint、Prettier，多人协作风格会漂移；
- **无 CI**：无 GitHub Actions / 任何流水线；
- git 历史只有 2 次提交，建议按功能小步提交；`.gitignore` 配置规范（密钥、产物、日志均已排除）；
- 前端 `serve.js` 与 `vite preview` 功能重复且更不安全，建议删除。

---

## 七、安全性汇总

| 风险 | 等级 | 处置建议 |
|---|---|---|
| API 密钥代理无鉴权，公网部署即裸奔 | 高 | 加简单 token 校验或 IP 白名单；至少不要公网暴露 |
| CORS `*` | 高 | `cors({ origin: 'http://localhost:5173' })` |
| serve.js 路径穿越 | 中 | 删除该文件 |
| 50mb body + 无速率限制 | 中 | 降到 10mb + express-rate-limit |
| 密钥管理 | ✅ 好 | `.env` 未入库，`.env.example` 模板规范 |

---

## 八、改进路线图（按优先级）

**P0 — 关乎论文成立（本周就该定方向）**
1. 确定检测有效性的补救路线（见第三节），决定系统定量指标由"LLM 生成"还是"CV 模型计算"；
2. 无论选哪条路，删掉或标注清楚分形维数、产状等"幻觉字段"。

**P1 — 关乎演示安全**
3. 后端加鉴权 + 收紧 CORS + 速率限制；
4. 删除 serve.js，PDF 导出补上标注图。

**P2 — 关乎代码长期健康**
5. 拆分 DetectPage.vue；路由懒加载；
6. 补 `extractJSON`/`normalizeCracks` 单元测试 + ESLint；
7. 检测结果从 localStorage 迁移到后端持久化（SQLite 即可），支撑论文的"案例库"叙事。

---

## 九、结语

这套系统的**工程完成度在同类暑期实践项目中属于上乘**——全链路跑得通、容错有层次、细节有打磨（画布对齐、资源释放、错误分级）。真正的短板不在代码，而在方法论：用通用多模态大模型零样本输出定量地质指标，目前本质上是"看起来很科学的幻觉"。好在架构上 LLM 与标注、展示层解耦得不错，替换或叠加一个真正的 CV 检测模型不需要推翻重写。尽早做这个决定，论文会扎实得多。
